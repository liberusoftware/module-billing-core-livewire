# Billing Core Livewire

Livewire 4 adapter for the Billing Core account workflow. The component uses
the core action and query, validates browser state at the boundary, and keeps
team-scoped data out of public state.
